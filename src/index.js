import React, {Fragment} from 'react';
import {Text} from 'react-native';
import Testo from '~/text';

const App = () => {
    return (
        <Fragment>
            <Testo />
        </Fragment>
    );
};

export default App;
